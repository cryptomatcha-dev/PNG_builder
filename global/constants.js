import path from 'path'

export const OUTPUT_DIR = path.join(process.cwd(), 'output')
export const LAYERS_DIR = path.join(process.cwd(), 'layers')
export const PNGS_DIR = path.join(OUTPUT_DIR, 'pngs')
export const META_DIR = path.join(OUTPUT_DIR, 'meta')
